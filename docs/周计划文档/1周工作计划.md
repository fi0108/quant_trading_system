
第1周迭代：Hello World交易版
迭代目标
核心目标

在IBKR纸盘账户成功下出第一个订单，并看到成交

成功标准

能连接IBKR纸盘账户
能接收AAPL实时数据
策略能产生买入信号
订单能提交到IBKR
在IBKR界面看到订单和成交
不追求（技术债）

代码优雅（先跑起来）
异常处理完善（只处理关键异常）
性能优化（功能优先）
测试覆盖（手动测试为主）
各模块实现范围
数据层（90%→60%精简版）
本周只实现

数据抽象（保留已完成）

Symbol、Bar、Slice对象保持不变
已经可用，不需要改动
IBKR实时数据源（精简版）

连接IBKR
订阅单个标的（AAPL）
接收5秒K线数据
推送：每收到1根5秒K线立即创建Slice推送
DataManager（极简版）

只支持实盘模式
只支持单个标的
功能：连接IBKR + 订阅AAPL + 推送Slice
不实现

历史数据获取
数据库数据源
回测模式
多标的支持
数据质量检查
Redis缓存
关键设计决策

决策1：5秒推送 vs 1分钟推送

选择：5秒（IBKR的reqRealTimeBars原生支持）
理由：快速看到效果，便于调试
决策2：是否累积成1分钟K线

选择：不累积，直接推送5秒K线
理由：简化逻辑，策略自己决定用多少数据
工作量分配

调整IBKR数据源：2小时
调整DataManager：3小时
测试验证：2小时
小计：7小时
策略层（0%→20%极简版）
本周只实现

固定买入策略

策略名称：DummyBuyStrategy
策略逻辑：每收到10次数据买入1股AAPL
不管价格、不管持仓、不管任何条件
策略接口

类：DummyBuyStrategy
属性：count计数器
方法：on_data接收Slice，返回Signal或None
逻辑：count能被10整除时返回买入信号
信号对象

字段：symbol（标的）、action（BUY/SELL）、quantity（数量）
就这三个字段
不实现

策略基类
技术指标计算（均线、MACD等）
策略参数配置
多策略支持
策略状态管理
策略回测
关键设计决策

决策1：为什么不做均线策略

理由：需要历史数据，增加复杂度
本周目标：先让流程跑通
决策2：为什么每10次就买入

理由：5秒推送一次，10次=50秒，容易观察
便于测试：运行2-3分钟就能看到多次买入
工作量分配

实现DummyBuyStrategy：1小时
实现Signal对象：0.5小时
测试验证：0.5小时
小计：2小时
订单管理层（0%→20%极简版）
本周只实现

订单创建和提交

功能：接收Signal，转换成IBKR订单
订单类型：只支持市价单
提交方式：调用IBKR的placeOrder
返回结果：打印订单ID
订单对象

order_id：订单ID（IBKR返回）
symbol：AAPL
action：BUY
quantity：1
order_type：MARKET
status：SUBMITTED
OrderManager类

方法：place_order(signal)
流程：创建IBKR Contract → 创建MarketOrder → 调用placeOrder → 打印订单ID → 返回订单对象
不实现

订单状态跟踪
限价单
订单取消
订单历史记录
订单幂等性
订单超时处理
关键设计决策

决策1：为什么只用市价单

理由：100%成交，不会出现未成交的复杂情况
缺点：有滑点，但纸盘无所谓
决策2：为什么不跟踪订单状态

理由：市价单几乎秒成交，不需要跟踪
简化：假设订单都成功了
决策3：可能重复下单怎么办

接受：本周可以重复下单
技术债：记录到下周解决
工作量分配

实现Order对象：0.5小时
实现OrderManager：2小时
对接IBKR API：1.5小时
测试验证：1小时
小计：5小时
风控层（0%→10%极简版）
本周只实现

资金检查

检查逻辑：账户现金 > 200美元
检查时机：下单前
检查结果：通过/拒绝
RiskControl类

方法：check_order(signal)
流程：查询IBKR账户现金 → 判断cash > 200 → 返回True/False
拒绝处理

拒绝时打印日志"资金不足，拒绝下单"
不抛异常，只是跳过这次下单
不实现

持仓限额检查
交易频率限制
止损止盈
当日亏损限制
风控规则配置
关键设计决策

决策1：为什么只检查现金

理由：最基本的风控，至少保证有钱
足够：本周只买不卖，不会爆仓
决策2：为什么是200美元

理由：AAPL约175美元，200美元能买1股
写死：够用，不需要配置
决策3：拒绝后怎么办

策略继续运行，等下次信号
不停止系统（可能后续有钱了）
工作量分配

实现RiskControl类：1小时
对接IBKR账户查询：1小时
集成到订单流程：0.5小时
测试验证：0.5小时
小计：3小时
持仓管理层（0%→20%极简版）
本周只实现

持仓记录（内存中）

数据结构：字典 {symbol: quantity}
初始值：{"AAPL": 0}
不存数据库，程序关闭就丢失
PositionManager类

方法：update_position(symbol, quantity)
更新持仓：positions[symbol] += quantity
打印日志："AAPL持仓更新：5 → 6"
方法：get_position(symbol)
返回持仓数量
更新时机

时机：订单提交后（假设都成交）
不等成交回报
直接更新：position += 订单数量
不实现

持仓盈亏计算
持仓对账
持仓历史记录
持仓市值计算
成交回报监听
关键设计决策

决策1：为什么不等成交回报

理由：市价单几乎秒成交
简化：假设订单提交=成交
技术债：下周增加成交回报监听
决策2：为什么不存数据库

理由：本周只跑30分钟测试
不需要：程序关闭就重来
技术债：第3周增加持久化
决策3：为什么不对账

理由：本周不会出现不一致
技术债：第3周增加对账
工作量分配

实现PositionManager类：1小时
集成到订单流程：0.5小时
测试验证：0.5小时
小计：2小时
主程序集成（0%→100%）
本周要实现

主程序入口

文件：main.py
流程：
步骤1：创建各模块实例
步骤2：定义on_data回调函数
调用strategy.on_data获取信号
如果有信号，调用risk_control.check_order检查
通过后调用order_manager.place_order下单
下单后调用position_manager.update_position更新持仓
步骤3：启动系统
启动脚本和配置

配置文件：config.yaml
内容：
IBKR配置：host、port、client_id
策略配置：name、trigger_interval
风控配置：min_cash
日志配置

日志级别：INFO
输出格式：时间 - 模块 - 级别 - 消息
输出位置：控制台（不存文件）
示例日志：
收到AAPL数据: 175.20
产生买入信号: AAPL 1股
风控通过
订单已提交: ID=12345
AAPL持仓: 0 → 1
工作量分配

实现main.py：2小时
配置文件和日志：1小时
集成调试：2小时
小计：5小时
本周总工作量估算
模块	工作量
数据层精简	7小时
策略层实现	2小时
订单管理层实现	5小时
风控层实现	3小时
持仓管理层实现	2小时
主程序集成	5小时
测试和调试	6小时
文档记录	2小时
总计	32小时
时间安排

每天工作6-7小时 × 5天 = 30-35小时
建议：周一到周五，每天晚上2小时 + 周末各8小时
验收标准
功能验收
验收脚本

准备工作

启动IBKR TWS纸盘账户
确认API已启用
确认账户有足够现金（>$1000）
运行系统

打开终端
运行：python main.py
观察日志输出
预期观察（运行5分钟）

时间	日志内容	验收项
00:05	连接IBKR成功	✓ 连接成功
00:10	订阅AAPL成功	✓ 订阅成功
00:15	收到AAPL数据: 175.20	✓ 数据推送
00:20	收到AAPL数据: 175.25	✓ 持续推送
00:50	策略计数: 10, 产生买入信号	✓ 策略逻辑
00:50	风控检查: 现金=$2500, 通过	✓ 风控检查
00:50	订单已提交: ID=12345	✓ 订单提交
00:50	AAPL持仓: 0 → 1	✓ 持仓更新
01:40	策略计数: 20, 产生买入信号	✓ 再次买入
01:40	AAPL持仓: 1 → 2	✓ 持仓累积
IBKR界面验证

打开TWS，查看持仓
应该看到：AAPL 2股（或更多）
查看订单历史，应该有多个订单记录
核心验收点（必须全部通过）

 系统能连接IBKR
 能持续接收AAPL数据（每5秒）
 策略能产生买入信号（每50秒）
 风控检查能通过
 订单能提交成功
 IBKR界面能看到持仓
 持仓数量与预期一致
可接受的问题（技术债）

⚠️ 可能偶尔重复下单（幂等性问题）
⚠️ 持仓数量可能与IBKR不一致（没对账）
⚠️ 系统崩溃无法恢复（没异常处理）
⚠️ 日志可能不够详细
稳定性验收
最低要求

能稳定运行5分钟不崩溃
能成功下2-3个订单
不要求

长时间运行（下周再说）
异常恢复（下周再说）
断线重连（下周再说）
关键决策总结
决策1：极简优先 vs 功能完整
选择：极简优先

理由

本周目标是"跑通流程"，不是"功能完整"
宁可功能少，也要快速看到效果
80%的功能都可以下周加
具体体现

只支持1个标的（AAPL）
只支持市价单（不要限价单）
只检查现金（不检查持仓限额）
假设订单都成交（不监听回报）
决策2：内存存储 vs 数据库持久化
选择：内存存储

理由

本周只运行5分钟，不需要持久化
程序重启就重来，没关系
节省时间，专注核心流程
技术债

第3周增加数据库持久化
届时支持系统重启恢复
决策3：市价单 vs 限价单
选择：市价单

理由

市价单100%成交，不需要处理未成交情况
简化订单状态跟踪逻辑
纸盘测试，滑点无所谓
代价

会有滑点（可能多花几美分）
无法控制成交价格
技术债

第2周增加限价单支持
决策4：假设订单成交 vs 监听成交回报
选择：假设订单成交

理由

市价单几乎秒成交（<1秒）
监听回报增加复杂度（需要异步处理）
本周只测试5分钟，出问题也能看出来
风险

如果订单被拒绝，持仓会不准
技术债记录：下周必须增加回报监听
权衡

用1%的风险，换50%的开发时间
决策5：固定策略 vs 灵活策略
选择：固定策略（写死）

理由

本周目标不是"做策略"，而是"通流程"
固定逻辑最简单，容易预测
便于验证：知道什么时候会买入
代价

策略不实用（只能买入）
无法配置参数
技术债

第2周实现真实策略（均线）
增加策略基类抽象
风险和应对
风险1：IBKR连接不稳定
现象

连接超时
断线频繁
应对

检查TWS是否正确启动
检查API设置是否启用
换用Gateway（更稳定）
增加重连逻辑（1小时工作量）
优先级：高（阻塞性问题）

风险2：市价单滑点大
现象

预期175美元成交
实际176美元成交
应对

纸盘测试，接受滑点
记录技术债：第2周增加限价单
优先级：低（不影响验证）

风险3：订单被IBKR拒绝
现象

订单提交后被拒绝
可能原因：资金不足、标的错误
应对

查看IBKR拒绝原因
检查账户配置
增加订单拒绝日志（0.5小时）
优先级：中（影响验收）

风险4：持仓不一致
现象

系统显示持仓2股
IBKR显示持仓3股
应对

本周接受不一致（技术债）
手动对比确认问题
第3周增加自动对账
优先级：低（预期内的问题）

本周交付物
代码文件

项目结构：
src/
├── data/
│   ├── abstractions.py          ✅ 已有
│   ├── data_manager.py          🔧 精简版
│   └── sources/
│       └── ibkr_source.py       🔧 精简版
├── strategy/
│   ├── dummy_buy_strategy.py    ✨ 新建
│   └── signal.py                ✨ 新建
├── order/
│   ├── order.py                 ✨ 新建
│   └── order_manager.py         ✨ 新建
├── risk/
│   └── risk_control.py          ✨ 新建
├── position/
│   └── position_manager.py      ✨ 新建
└── main.py                       ✨ 新建

配置文件：
├── config.yaml                   ✨ 新建
└── requirements.txt              🔧 更新

说明：
✅ 已有 - 之前完成的
🔧 修改 - 需要精简或调整
✨ 新建 - 本周新增
文档

docs/
├── week1_iteration_summary.md    # 本周迭代总结
├── tech_debt.md                  # 技术债清单
└── week1_demo_video.md           # 演示录屏链接（可选）
技术债清单
高优先级（第2周必须解决）

 订单幂等性（防止重复下单）
 订单成交回报监听（不能假设都成交）
 增加限价单支持（减少滑点）
中优先级（第2-3周解决）

 多标的支持（至少3个）
 数据异常处理（网络断开、数据缺失）
 持仓对账（与IBKR定期对账）
低优先级（第4周及以后）

 数据库持久化
 单元测试
 性能优化
每日工作计划（建议）
Day 1（周一）- 6小时
上午（3小时）

精简DataManager：1小时
精简IBKR数据源：1小时
测试数据推送：1小时
下午（3小时）

实现DummyBuyStrategy：1小时
实现Signal对象：0.5小时
测试策略逻辑：0.5小时
设计整体架构：1小时
目标：数据能推送，策略能产生信号

Day 2（周二）- 7小时
上午（3.5小时）

实现Order对象：0.5小时
实现OrderManager：2小时
对接IBKR下单API：1小时
下午（3.5小时）

测试订单提交：1小时
实现RiskControl：2小时
测试风控检查：0.5小时
目标：能下单到IBKR

Day 3（周三）- 6小时
上午（3小时）

实现PositionManager：1.5小时
实现main.py主程序：1.5小时
下午（3小时）

配置文件和日志：1小时
集成所有模块：2小时
目标：完整流程跑通

Day 4（周四）- 6小时
上午（3小时）

端到端测试：2小时
修复bug：1小时
下午（3小时）

继续测试：1小时
优化日志输出：1小时
完善异常处理：1小时
目标：稳定运行5分钟

Day 5（周五）- 6小时
上午（2小时）

最终验收测试
录制演示视频（可选）
下午（4小时）

编写迭代总结文档：2小时
整理技术债清单：1小时
代码提交和归档：1小时
目标：完成交付

成功的关键
关键1：抵制诱惑
诱惑：既然要做订单管理，不如把限价单也做了吧？

抵制

本周目标是"跑通流程"
限价单增加30%工作量
市价单够用了
坚持：下周再做
原则：每个功能都问自己"本周不做会死吗？"

关键2：快速反馈
不要

写完所有代码再测试
等所有模块都完成再集成
要

每完成一个模块，立即测试
随时可运行的系统
每天都能看到进展
方法

Day1结束：数据能推送
Day2结束：能下单
Day3结束：完整流程通
关键3：记录技术债
每发现一个"本周不做"的问题，记录到tech_debt.md

格式

问题描述
为什么不做
何时做
优先级
好处

不会忘记
下周有清单
不纠结
总结
第1周的核心
目标：下单成功（不追求完美）
策略：极简优先（能少一个功能就少一个）
验收：运行5分钟，看到持仓增加
心态：接受不完美，拥抱技术债
预期结果
代码质量：60分（能跑）
功能完整度：20%（只有核心流程）
成就感：100分（能下单了！）
信心：爆棚（原来量化交易也没那么难）
下周预告
增加卖出逻辑（完整交易循环）
增加限价单（减少滑点）
增加订单跟踪（监听成交回报）
增加持仓对账（与IBKR同步）
支持3个标的（AAPL/TSLA/GOOGL）